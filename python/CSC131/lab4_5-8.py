import math

# a function that takes the argument of a radius and returns the area of the corresponding circle 
def areaOfCircle(r):
    return math.pi*math.pow(r, 2)

# for testing purposes
#print(areaOfCircle(5))
